# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# Vector DB Configuration (Primary Database)
VECTORDB_PATH=/vectordb/chroma_db
COLLECTION_NAME=your_collection_name
EMBEDDING_MODEL=gemini

# Vector DB Configuration (Secondary Database - Optional)
# Uncomment and configure if you need multiple data sources
# DB2_VECTORDB_PATH=/vectordb/secondary_db
# DB2_COLLECTION_NAME=secondary_collection
# DB2_EMBEDDING_MODEL=gemini

# Server Configuration
PORT=8080
GUNICORN_WORKERS=4
LOG_LEVEL=info

# Docker Configuration (for docker-compose)
VECTORDB_HOST_PATH=./vectordb

# Optional: External Service URL
# SERVICE_URL=https://your-service.com